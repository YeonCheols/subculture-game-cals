const { app, BrowserWindow, Menu, Notification, Tray, ipcMain, shell, nativeImage } = require("electron");
const path = require("path");
let mainWindow; let tray;
function createWindow(){mainWindow=new BrowserWindow({width:1440,height:1024,minWidth:900,minHeight:650,show:!process.env.CAPTURE_PATH,backgroundColor:"#06111f",title:"게임타임",webPreferences:{preload:path.join(__dirname,"preload.cjs"),contextIsolation:true,nodeIntegration:false}});const devUrl=process.env.VITE_DEV_SERVER_URL;if(devUrl)mainWindow.loadURL(devUrl);else mainWindow.loadFile(path.join(__dirname,"../dist/client/index.html"));mainWindow.webContents.once("did-finish-load",async()=>{if(process.env.CAPTURE_PATH){await new Promise(resolve=>setTimeout(resolve,1200));const image=await mainWindow.webContents.capturePage();require("fs").writeFileSync(process.env.CAPTURE_PATH,image.toPNG());app.isQuitting=true;app.quit()}});mainWindow.on("close",event=>{if(!app.isQuitting){event.preventDefault();mainWindow.hide()}})}
function createTray(){if(process.platform!=="darwin")return;const icon=nativeImage.createFromNamedImage("NSActionTemplate");if(icon.isEmpty())return;icon.setTemplateImage(true);tray=new Tray(icon);tray.setToolTip("게임타임 - 오늘의 게임 일정");tray.setContextMenu(Menu.buildFromTemplate([{label:"게임타임 열기",click:()=>{mainWindow.show();mainWindow.focus()}},{label:"오늘 일정 새로고침",click:()=>mainWindow.webContents.reload()},{type:"separator"},{label:"종료",click:()=>{app.isQuitting=true;app.quit()}}]));tray.on("double-click",()=>mainWindow.show())}
app.whenReady().then(()=>{createWindow();ipcMain.handle("open-external",(_,url)=>shell.openExternal(url));ipcMain.handle("test-notification",(_,title,body)=>new Notification({title,body}).show())});app.on("window-all-closed",()=>{if(process.platform!=="darwin")app.quit()});app.on("before-quit",()=>{app.isQuitting=true});
